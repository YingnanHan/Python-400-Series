# 并集  union() |
# 交集  intersection()  &
# 差集  diffrence() -
# 对称差 symmetric_difference()

# 含有update的方法指的是将最终的的结果

a = {1, 2, 3, 4}
b = {2, 5, 6, 7}
print(a.union(b))
print(a.intersection(b))
print(a.difference(b))
print(a.symmetric_difference(b))
